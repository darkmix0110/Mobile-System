import 'package:flutter/material.dart';

class Genre {
  final String id, name;
  final Color color;
  final IconData icon;
  const Genre({required this.id, required this.name, required this.color, required this.icon});
}